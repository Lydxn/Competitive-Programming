#include <bits/stdc++.h>

using namespace std;

int main()
{
	cin.sync_with_stdio(0);
	cin.tie(0);

	int N, D, Q, L, T;
	cin >> N >> D >> Q >> L >> T;

	cout << 5 * N + 10 * D + 25 * Q + 100 * L + 200 * T << '\n';
	return 0;
}
