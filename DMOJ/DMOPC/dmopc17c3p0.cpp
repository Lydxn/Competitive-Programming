#include <bits/stdc++.h>

using namespace std;

int main() {
	cin.sync_with_stdio(0);
	cin.tie(0);

	double B;
	cin >> B;

	cout << fixed << setprecision(2) << '$' << B / 2 << " $" << B / 2 << '\n';
	return 0;
}
