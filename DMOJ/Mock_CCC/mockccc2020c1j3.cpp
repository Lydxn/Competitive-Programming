#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef pair<int, int> pii;

int main() {
    cin.sync_with_stdio(0);
    cin.tie(0);

    long double X, Y;
    cin >> X >> Y;

    cout << fixed << setprecision(2) << X * Y / 4.0 << '\n';
    return 0;
}
