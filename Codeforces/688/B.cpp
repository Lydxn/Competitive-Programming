#include <bits/stdc++.h>

using namespace std;

int main()
{
    cin.sync_with_stdio(0);
    cin.tie(0);
    
    string n;
    cin >> n;
    
    cout << n;
    reverse(n.begin(), n.end());
    cout << n;
    return 0;
}
