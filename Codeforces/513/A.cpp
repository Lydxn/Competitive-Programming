#include <bits/stdc++.h>

using namespace std;

int main()
{
    cin.sync_with_stdio(0);
    cin.tie(0);

    int n1, n2;
    cin >> n1 >> n2;
    
    cout << (n1 > n2 ? "First" : "Second") << '\n';
    return 0;
}
