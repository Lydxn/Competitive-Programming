#include <bits/stdc++.h>

using namespace std;

int main()
{
    cin.sync_with_stdio(0);
    cin.tie(0);
    
    int t, x, ans;
    cin >> t;
    
    while (t--)
    {
        cin >> x;
        cout << x / 2 << '\n';
    }
    return 0;
}
