#include <bits/stdc++.h>

using namespace std;

int main() {
	cin.sync_with_stdio(0);
	cin.tie(0);

	double n, m, p, q, slope; int dir = 3, cnt = 1;
	cin >> n >> m >> p >> q;

	slope = q / (n - p);
	while (true) {
		switch (dir) {
			case 0:
				break;
			case 1:
				break;
			case 2:
				break;
			case 3:
				break;
		}
	}
	return 0;
}
